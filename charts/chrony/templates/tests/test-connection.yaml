apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chrony.fullname" . }}-test-connection"
  labels:
    {{- include "chrony.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chrony.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
